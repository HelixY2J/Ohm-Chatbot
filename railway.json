{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python -m pip install -r requirements.txt && python backend/manage.py collectstatic --noinput --settings=core.settings.production && python backend/manage.py migrate --settings=core.settings.production && gunicorn --workers=4 core.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
    }
  }